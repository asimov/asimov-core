
module.exports = function (grunt) {
    'use strict';

    grunt.registerTask(
        'prepare',
        'Prepare for compilation',
        function (env) {
            // do stuff
        }
    );
};
