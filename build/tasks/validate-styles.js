
module.exports = function (grunt) {
    'use strict';

    grunt.registerTask(
        'validate-styles',
        'Validates styles',
        function (env) {
            env = env || 'prod';
        }
    );
};
